<Window x:Class="KHZ.Sheet.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KHZ Sheet"
        Width="1440"
        Height="900"
        MinWidth="1000"
        MinHeight="650"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource GridBrush}"
                BorderThickness="0,0,0,1"
                Padding="18,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="KHZ Sheet"
                               FontSize="22"
                               FontWeight="SemiBold"/>
                    <TextBlock Text="Deterministic local-first workbook"
                               Foreground="{StaticResource MutedTextBrush}"
                               Margin="0,2,0,0"/>
                </StackPanel>

                <Border Grid.Column="1"
                        Background="#122018"
                        BorderBrush="{StaticResource AccentBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="10,4"
                        VerticalAlignment="Center">
                    <TextBlock Text="PRODUCTIVITY / SPREADSHEET / LOCAL-FIRST"
                               Foreground="#7EE787"
                               FontSize="11"
                               FontWeight="SemiBold"/>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource GridBrush}"
                BorderThickness="0,0,0,1"
                Padding="12,8">
            <WrapPanel>
                <Button Content="New Sheet" Click="NewSheet_Click"/>
                <Button Content="Open CSV" Click="OpenCsv_Click"/>
                <Button Content="Save CSV" Click="SaveCsv_Click"/>
                <Button Content="Export XLSX" Click="ExportXlsx_Click"/>
                <Separator Width="12" Opacity="0"/>
                <Button Content="Recalculate" Click="Recalculate_Click"/>
                <Button Content="Verify Proof" Click="Verify_Click" Style="{StaticResource AccentButton}"/>
            </WrapPanel>
        </Border>

        <Border Grid.Row="2"
                Background="{StaticResource PanelRaisedBrush}"
                BorderBrush="{StaticResource GridBrush}"
                BorderThickness="0,0,0,1"
                Padding="10,7">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="36"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="NameBox"
                         Grid.Column="0"
                         IsReadOnly="True"
                         Text="A1"
                         HorizontalContentAlignment="Center"
                         FontWeight="SemiBold"/>

                <TextBlock Grid.Column="1"
                           Text="fx"
                           FontStyle="Italic"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource AccentHoverBrush}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>

                <TextBox x:Name="FormulaBox"
                         Grid.Column="2"
                         KeyDown="FormulaBox_KeyDown"
                         ToolTip="Enter a value or formula, for example =SUM(A1:A10)"/>

                <Button Grid.Column="3"
                        Content="Apply"
                        Click="ApplyFormula_Click"
                        Margin="8,0,0,0"
                        Style="{StaticResource AccentButton}"/>
            </Grid>
        </Border>

        <DataGrid x:Name="SheetGrid"
                  Grid.Row="3"
                  ItemsSource="{Binding}"
                  AutoGenerateColumns="True"
                  RowHeaderWidth="52"
                  RowHeight="28"
                  ColumnWidth="110"
                  FrozenColumnCount="0"
                  LoadingRow="SheetGrid_LoadingRow"
                  AutoGeneratingColumn="SheetGrid_AutoGeneratingColumn"
                  SelectedCellsChanged="SheetGrid_SelectedCellsChanged"
                  CellEditEnding="SheetGrid_CellEditEnding"
                  PreviewKeyDown="SheetGrid_PreviewKeyDown"
                  SelectionUnit="Cell"
                  SelectionMode="Extended"
                  ScrollViewer.CanContentScroll="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"/>

        <Border Grid.Row="4"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource GridBrush}"
                BorderThickness="0,1,0,0"
                Padding="8,6">
            <DockPanel LastChildFill="True">
                <Button DockPanel.Dock="Left"
                        Content="+"
                        Width="32"
                        Height="28"
                        Padding="0"
                        Click="NewSheet_Click"
                        ToolTip="Add sheet"/>

                <ListBox x:Name="SheetList"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="{StaticResource TextBrush}"
                         SelectionChanged="SheetList_SelectionChanged"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="16,6"/>
                            <Setter Property="Margin" Value="0,0,4,0"/>
                            <Setter Property="Background" Value="{StaticResource PanelRaisedBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="TabBorder"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{StaticResource GridBrush}"
                                                BorderThickness="1"
                                                CornerRadius="4"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                                <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Border>

        <Border Grid.Row="5"
                Background="#090C10"
                BorderBrush="{StaticResource GridBrush}"
                BorderThickness="0,1,0,0"
                Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="StatusText"
                           Foreground="{StaticResource MutedTextBrush}"
                           Text="Ready"/>

                <TextBlock x:Name="EngineText"
                           Grid.Column="1"
                           Foreground="{StaticResource MutedTextBrush}"
                           Text="engine"/>
            </Grid>
        </Border>
    </Grid>
</Window>
